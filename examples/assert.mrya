// Assertions can be used for unit testing

// Let's define x, we will use it later
let x = 0

// Raise an exception if 2+2 != 4, but it is here
assert(2 + 2, 4)

// Raise an exception if they are not equal (they are here)
assert("jaskdjaskdjksal", "jaskdjaskdjksal")

// increment X by 1
x = x + 1

// True == True no exception here
assert(true, true)

// We incremented X by 1 on line 13, and it should be 1 here!
assert(x, 1)

// And this should fail because 3.3 != 3.69
assert(3.3, 3.69)